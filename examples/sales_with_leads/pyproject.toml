[project]
name = "sales-with-leads"
version = "0.1.0"
description = "Sales agent with stateful leads extraction and company research"
requires-python = ">=3.10"
dependencies = [
    "cartesia-line==0.2.2",
    "loguru>=0.7.0",
]

[tool.setuptools]
py-modules = ["main"]
