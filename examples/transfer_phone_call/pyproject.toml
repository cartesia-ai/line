[project]
name = "transfer-phone-call"
version = "0.1.0"
description = "Phone transfer example using Line SDK v02"
requires-python = ">=3.10"
dependencies = [
    "cartesia-line==0.2.0.dev0",
]

[tool.setuptools]
py-modules = ["main"]
