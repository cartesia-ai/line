[project]
name = "transfer-agent"
version = "0.1.0"
description = "Multi-agent handoff example demonstrating agent-to-agent transfers using Line SDK v02"
requires-python = ">=3.10"
dependencies = [
    "cartesia-line==0.2.0.dev1",
]

[tool.setuptools]
py-modules = ["main"]
