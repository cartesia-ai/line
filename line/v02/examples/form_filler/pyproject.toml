[project]
name = "form-filler"
version = "0.1.0"
description = "Form filling example using Line SDK v02"
requires-python = ">=3.10"
dependencies = [
    "cartesia-line==0.2.0.dev0",
]

[project.scripts]
form-filler = "main:app.run"

[tool.setuptools]
py-modules = ["main", "form_filler"]
