[project]
name = "echo"
version = "0.1.0"
description = "Handoff echo example using Line SDK v02"
requires-python = ">=3.10"
dependencies = [
    "cartesia-line",
]

[project.scripts]
form-filler = "main:app.run"

[tool.uv.sources]
cartesia-line = { path = "../../../../", editable = true }
